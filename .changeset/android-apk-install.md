---
"tehran-metro": minor
---

Ship a Trusted Web Activity APK (targetSdk 35) for Android, detect an already-installed app so the site shows update (via GitHub CHANGELOG) instead of install, and compare versions inside the installed app on open.
